buildscript {
    repositories {
        mavenCentral()
        mavenLocal()
    }
    dependencies {
        classpath 'com.mparticle:android-kit-plugin:' + project.version
    }
}

apply plugin: 'com.mparticle.kit'

repositories {
    maven {
        url 'http://maven.singular.net'
    }
}

android {
    defaultConfig {
        minSdkVersion 14
    }
}

dependencies {
    compile 'com.singular.sdk:singular_sdk:7.3.3'
}